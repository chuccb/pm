# 儲存庫文件與研究規範

本文件是 `chuccb/pm` 的文件維護規則。人員、LLM、代理程式與自動化工具修改本儲存庫時，都必須遵守。

## 1. 語言規則：只使用繁體中文

所有 Markdown 的「人類可讀說明文字」一律使用繁體中文，包含：

- 標題、段落、表格欄位與表格說明。
- 註解、注意事項、研究結論與 TODO。
- README、研究筆記、規格、設計說明與提交前的文件內容。

不得在一般說明文字中使用簡體中文。

下列內容可以原樣保留，不視為文件語言違規：

- 程式碼、組合語言、封包名稱、Opcode、函式名稱、類別名稱、變數名稱。
- API、函式庫、工具、產品、檔案名稱、路徑、網址與外部識別字串。
- 必須逐字保存的原始字串、玩家名稱、遊戲內文字、日文原文或其他研究證據；應明確標示為原文。

說明文字即使混合英文技術術語，也必須以繁體中文敘述。

## 2. 文件分層

### 根目錄

`README.md` 只負責儲存庫定位、研究入口與高階原則，不放大型逆向細節。

`AGENTS.md` 是本儲存庫的強制維護規範。

### `Research/`

`Research/README.md` 是研究總覽。

`Research/DOCUMENT_INDEX.md` 是所有研究 Markdown 的唯一索引與分類入口。新增、刪除、合併或重新命名研究文件時，必須同步更新它。

### `Research/Core/`

集中保存跨系統的核心協定、狀態機、玩家、房間、戰鬥、資源與欄位研究。

### `Research/KickVote/`

集中保存 Kick Vote 的完整研究；其內部 README 作為該主題的局部入口。

目前先維持既有路徑，不為了形式上的「整理」任意搬移舊文件，以避免大量破壞既有連結與研究引用。真正需要合併時，應先確認內容重疊程度，再以 Git 歷史保留可追溯性。

## 3. 一個概念只能有一份主要真相

新增文件前先搜尋既有研究。

若主題已存在：

1. 優先更新既有主文件。
2. 只有在內容屬於不同證據層、不同協定層或不同生命週期時才建立新文件。
3. 新文件必須在 `Research/DOCUMENT_INDEX.md` 註明它與既有文件的關係。

不得因「這次研究角度不同」就複製整份舊文件形成兩份互相漂移的版本。

## 4. 檔名規則

研究文件檔名使用穩定、可搜尋的英文 ASCII 名稱；多個概念使用底線分隔。

既有後綴的語意固定如下：

- `README.md`：目錄入口與導覽，不作為唯一的細節證據庫。
- `Protocol.md`：封包、協定或資料流。
- `Schema.md`：欄位、資料結構與型別表。
- `Field_Evidence.md`：欄位直接證據與觀察結果。
- `DeepEvidence.md`：需要跨函式、跨資源或跨來源的深入證據鏈。
- `Detail.md`：單一封包或子功能的細節研究。
- `Lifecycle.md`：生命週期、狀態轉移與事件順序。
- `State.md`：狀態資料、狀態機或結果狀態。

若既有文件名稱已經穩定使用，不要為了追求形式一致而頻繁改名。

## 5. 研究證據規則

每個重要結論都必須能回答「證據從哪裡來」。本專案使用以下標記：

- `[C]`：`PaperMan.exe.c` 可直接觀察的反編譯證據。
- `[RES]`：`Extracted/` 中的資源或資料檔證據。
- `[WIKI]`：日本 PaperMan Wiki 的歷史玩家可見資料。
- `[X]`：兩種以上獨立來源互相吻合的交叉驗證。
- `[OPEN]`：目前仍未充分證明。

證據優先順序不是「看起來合理」；應優先回到 serializer、parser、caller/callee、實際讀寫寬度、資料流與資源定義。

Hex-Rays 猜出的函式原型、變數型別或變數名稱不得單獨視為真實語意。

## 6. Packet 研究規則

任何封包欄位的語意、寬度、符號性、編碼或順序，都要盡可能從以下鏈路確認：

```text
IDA C / LST
    -> serializer / parser
    -> caller / callee
    -> state-field read / write
    -> Extracted resource
    -> Wiki / 歷史行為
    -> 交叉驗證
```

不能因欄位「看起來像」玩家 ID、時間、狀態或數量，就直接命名為該語意。

尚未確認的欄位必須標 `[OPEN]`，不得為了讓伺服器程式能編譯而偷偷填入 `0`、固定常數或猜測值。

若最終實作確實需要預設值，必須在文件中明確寫出「這是相容性假設，不是逆向確認」。

## 7. 版本規則

主要研究目標固定為：

```text
日本版 PaperMan
2016 年服務終了時的最終 Client
```

任何歷史資料都必須盡可能記錄日期、版本或來源範圍。

不同年度、不同地區、不同 Client 的同名封包、模式、資源或規則，不得直接視為相同實作。

較舊版本資料可以作為演進、旁證或反證，但不能無標記地覆蓋 2016 final Client 的直接證據。

## 8. Markdown 結構規則

每份文件都應：

- 有一個清楚的第一層標題 `#`。
- 先寫「這份文件回答什麼問題」，再寫證據與細節。
- 使用一致的 `##`、`###` 階層，不跳級。
- 大型欄位資料優先使用表格；流程優先使用 ASCII/Markdown 流程圖；程式碼優先放程式碼區塊。
- 不重複貼整段 `PaperMan.exe.c`；保留最少必要片段，並指向函式或位址。
- 長篇推導應拆成「觀察 → 推論 → 結論 → 尚待確認」，不要把四者混成一段。

## 9. 更新規則

修改研究內容時，至少同步檢查：

1. 該主題的主文件。
2. `README.md` 或相關索引是否仍正確。
3. `Research/DOCUMENT_INDEX.md` 是否需要更新。
4. 受到結論影響的其他文件是否產生矛盾。

發現舊結論被新證據推翻時，不要默默刪掉歷史；應改寫為「原結論 → 新證據 → 修正後結論」，讓研究演進可追蹤。

## 10. 研究狀態不可混寫

文件中應明確區分：

```text
已直接確認
    !=
多來源交叉推導
    !=
高度可能
    !=
尚未確認
```

不要因為多個弱證據吻合，就把它升級成 `[C]`。

## 11. 自動檢查

Markdown 修改至少需要通過：

```text
python scripts/check_markdown.py
```

CI 也會執行相同檢查。

目前自動檢查的重點包含：

- 所有 Markdown 使用 UTF-8。
- 一般 Markdown 文字不得含已辨識的簡體中文轉換差異。
- 每份 Markdown 至少有一個 `#` 第一層標題。
- 標題階層不得任意跳級。
- 不允許明顯的重複 H1。

自動檢查不能取代人工研究審查；尤其無法自動判斷一個技術語意是否真的被 C、資源與 Wiki 證明。

## 12. LLM 與代理程式規則

任何 LLM、Coding Agent 或自動化研究工具讀取此儲存庫時，必須先閱讀：

```text
AGENTS.md
Research/README.md
Research/DOCUMENT_INDEX.md
```

之後再依工作主題進入 `Research/Core/`、`Research/KickVote/` 與 `Extracted/`。

LLM 不得：

- 自行捏造封包欄位名稱或數值。
- 將推測寫成 confirmed。
- 因為程式碼方便而刪掉 `OPEN`。
- 新增第二份內容高度重疊的研究文件。
- 使用簡體中文撰寫 Markdown 說明。

本規範的核心目標是讓研究可以長期累積，而不是每一輪重新整理同一批資訊。
