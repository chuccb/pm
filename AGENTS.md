# PaperMan 儲存庫維護與研究規則

本文件適用於所有人員、LLM、Coding Agent、自動化工具與後續研究工作。修改任何 Markdown、研究資料或研究索引前，必須遵守本規則。

## 1. 目標與版本基準

本儲存庫的主要研究目標固定為：

```text
日本版 PaperMan
2016 年服務終了時的最終 Client
```

主要證據來源：

- `PaperMan.exe`
- `PaperMan.exe.c`
- `PaperMan.exe.lst.zip`
- `Extracted/`
- 日本 PaperMan Wiki 與可追溯的歷史資料

不同年份、地區、Client 或資源版本不得直接混為同一實作。歷史資料可以作為版本演進、旁證或反證，但必須明確標示版本範圍。

## 2. 強制語言規則：Markdown 只能以繁體中文撰寫

所有 Markdown 的一般說明文字一律使用繁體中文，包括：

- 標題、段落、表格欄位與表格說明。
- 研究結論、推論、注意事項、TODO、註解與文件導覽。
- README、研究紀錄、協定說明、欄位說明與規格文件。

不得使用簡體中文撰寫一般說明，也不得以英文句子取代應有的繁體中文說明。

下列內容可維持原樣：

- 程式碼、組合語言、命令、正則表達式與程式識別字。
- 函式、類別、變數、結構、封包名稱、Opcode、常數與 API 名稱。
- 檔案名稱、路徑、URL、GitHub 識別字與工具名稱。
- 遊戲內原文、日文原文、原始字串及必須逐字保存的研究證據。

保留原文時，若其不是技術識別字，應在周圍使用繁體中文說明其性質與用途。

## 3. LLM／Agent 進入點

任何 LLM 或代理程式開始工作時，必須依序閱讀：

```text
AGENTS.md
Research/README.md
Research/DOCUMENT_INDEX.md
```

之後才進入相關主題文件與 `Extracted/`。

不可只讀單一摘要文件就修改研究結論。遇到衝突，必須回到原始 C、LST、資源與 Wiki 證據。

## 4. Markdown 文件層級與責任

### 根目錄

`README.md`：只負責儲存庫定位、研究入口、主要證據來源與高階規則；不要放大型逆向細節。

`AGENTS.md`：唯一的儲存庫級維護規則；不要再建立另一份互相平行的全域規則文件。

### `Research/`

`Research/README.md`：研究總覽、版本基準、證據方法、研究方向。

`Research/DOCUMENT_INDEX.md`：所有研究 Markdown 的唯一索引、分類與主文件關係。

### `Research/Core/`

核心遊戲、網路、玩家、房間、GameRule、模式、戰鬥、資源與封包研究。

現有 `Core` 路徑先維持不變，因為既有文件間存在大量互相連結。不要為了形式上的目錄漂亮而任意搬移既有文件。

### `Research/KickVote/`

Kick Vote 專題的唯一集中區。該目錄的 `README.md` 是主入口。

### `Extracted/`

遊戲資源與資料檔的原始材料區，不把研究結論直接寫進資源檔案名稱或資料內容。

## 5. 單一真相原則

同一概念、封包、狀態或欄位只能有一份主要真相。

新增文件前必須先搜尋既有文件。

若已有主文件：

1. 優先直接更新主文件。
2. 需要新增證據時，放入對應證據文件，而不是複製完整摘要。
3. 若確實需要新文件，必須在 `Research/DOCUMENT_INDEX.md` 說明它與既有文件的關係。

禁止建立以下形式的平行副本：

```text
New / Final / Final2 / Latest / V2 / Copy / Temp
```

也禁止因研究角度不同就重新複製同一份欄位表、封包表或結論。

## 6. 文件角色規則

新增或拆分文件前，先判斷它回答哪種問題：

| 問題 | 文件角色 |
|---|---|
| 完整生命週期與狀態轉移 | `*Lifecycle.md` |
| 封包建立、傳送、接收與整體格式 | `*Protocol.md` |
| 欄位型別、寬度、位置與資料結構 | `*Schema.md` |
| 單一欄位的直接證據與讀寫來源 | `*Field_Evidence.md` / `*Field_Semantics.md` |
| 跨函式、跨資源、跨來源的證據鏈 | `*DeepEvidence.md` |
| 單一封包或窄功能的深入拆解 | `*Detail.md` |
| 目前保存的狀態與狀態資料 | `*State.md` |
| 專題入口與導航 | `README.md` |

同一文件不得同時充當數個互相重疊的「第二主文件」。

## 7. 標準 Markdown 結構

每份 Markdown 必須：

1. 只有一個 `#` H1 標題。
2. H2、H3 依層級遞進，不任意跳級。
3. 開頭先說明「這份文件回答什麼問題」與適用版本。
4. 研究文件盡量依下列順序組織：

```text
目的／範圍
→ 目前結論
→ 直接觀察
→ 推論／交叉驗證
→ 欄位或流程細節
→ 未確認事項
→ 相關文件
```

5. 欄位資料優先使用表格；狀態與事件順序優先使用流程圖；原始程式片段使用程式碼區塊。
6. 不要重複貼大量 `PaperMan.exe.c`。應保留函式名稱、位址、最小必要片段與資料流說明。
7. 將「觀察」、「推論」、「結論」與「尚待確認」分開，不要混成沒有證據邊界的敘述。

## 8. 證據等級與措辭

研究結論必須能回答「證據從哪裡來」。統一使用：

- `[C]`：`PaperMan.exe.c` 可直接觀察的反編譯證據。
- `[RES]`：`Extracted/` 中的資源或資料檔證據。
- `[WIKI]`：日本 PaperMan Wiki 的歷史玩家可見資料。
- `[X]`：兩種以上獨立證據互相吻合的交叉驗證。
- `[OPEN]`：目前仍未充分證明。

`[C]` 只表示「C 檔直接出現證據」，不代表 Hex-Rays 猜出的名稱、型別或語意必然正確。

優先相信：

```text
實際 serializer / parser
→ 實際讀寫寬度
→ caller / callee 與資料流
→ state field 的讀寫
→ resource 定義
→ Wiki 行為
```

名稱相似、位置相近、數值看起來合理，都不足以單獨形成 confirmed 結論。

## 9. Packet 研究硬規則

任何封包欄位的以下資訊，都必須盡可能由證據確認：

```text
名稱
寬度
offset
signed / unsigned
編碼
順序
語意
來源
用途
```

標準追查鏈：

```text
IDA C / LST
    ↓
serializer / parser
    ↓
caller / callee
    ↓
state-field read / write
    ↓
Extracted resource
    ↓
Wiki / 歷史行為
    ↓
交叉驗證
```

尚未證明的欄位必須保留 `[OPEN]`。

不得為了讓 Server 編譯或看起來完整而把未知欄位偷偷改成 `0`、固定常數或猜測值。實作若確實需要預設值，必須明確標示：

```text
相容性假設，不是逆向確認
```

## 10. Server 重建規則

逆向研究的最終目的包含恢復可相容的 Server 行為，因此研究文件不能只記錄 Opcode。

涉及 Server 行為時，優先還原：

```text
連線／工作階段
→ 登入
→ Channel / Lobby
→ Room
→ Player / Slot / Team
→ GameRule
→ Match 狀態
→ Gameplay event
→ Result / Reward
→ Logout
```

每個重要狀態都要說明：

- 誰建立或修改它。
- 什麼事件觸發變更。
- 哪個封包同步它。
- Client 如何消費它。
- 還缺哪些證據。

## 11. 版本隔離

任何研究結論都要盡量帶出版本上下文。

```text
2016 Japan final Client
    !=
其他年份
    !=
其他地區 Client
```

舊版本 Wiki、封包編號或資源只有在明確說明用途時才能引用。不得把「歷史上曾經存在」寫成「2016 final Client 一定存在」。

## 12. Wiki、C、Resource 三方交叉驗證

主要功能應盡可能同時檢查：

```text
Wiki 行為
↕
IDA C / LST
↕
Extracted 資源
```

三者並非每次都必須全部找到，但缺少某一來源時應明確寫出缺口，而不是假裝已完成驗證。

若三方結果衝突：

1. 不要自行選一個「看起來對」的版本。
2. 分別記錄衝突來源與版本範圍。
3. 回到更底層的 serializer、parser、資料流與資源定義。
4. 暫時無法解決時保留 `[OPEN]`。

## 13. 索引同步規則

新增、刪除、合併、重新命名研究文件時，必須同步檢查：

```text
Research/README.md
Research/DOCUMENT_INDEX.md
相關主題 README
受影響的交叉連結
```

`Research/DOCUMENT_INDEX.md` 是研究文件的唯一導航索引。任何未在索引中出現的研究 Markdown，都視為尚未完成整理。

## 14. 新文件建立門檻

建立新 Markdown 前必須先回答：

```text
1. 既有哪一份文件已經處理這個主題？
2. 為什麼不能直接更新既有文件？
3. 新文件屬於哪個文件角色？
4. 它的證據來源是什麼？
5. 它要放進索引的哪一分類？
```

若不能回答第 2 題，原則上不要建立新文件。

## 15. 舊結論修正規則

新證據推翻舊結論時，不得默默刪除舊內容。

使用：

```text
原結論
→ 新證據
→ 為何不足／被推翻
→ 修正後結論
```

只有純文字整理、格式修正或明顯錯字才可以直接改寫而不保留研究演進說明。

## 16. 自動檢查

所有 Markdown 修改至少必須通過：

```text
python scripts/check_markdown.py
```

GitHub Actions 亦會執行相同檢查。

檢查至少涵蓋：

- UTF-8 編碼。
- 簡體中文轉換差異。
- 每份文件只有一個 H1。
- 標題層級不可任意跳級。

自動檢查只是最低門檻，無法取代人工證據審查。

## 17. 禁止事項

禁止：

- 使用簡體中文撰寫一般 Markdown 說明。
- 把推測寫成 confirmed。
- 把 Hex-Rays 猜測型別當成封包真實語意。
- 為未知欄位任意填 `0` 或固定值而不標記假設。
- 為同一概念建立第二份平行真相。
- 建立 `Final2`、`Latest`、`New` 等沒有研究語意的文件副本。
- 只更新細節文件而不更新索引與入口。
- 將其他版本／地區資料無標記地套入 2016 Japan final Client。
- 為了整理而任意大量搬移既有文件，造成連結與歷史脈絡損失。

## 18. 最終原則

```text
文件增加知識，不增加副本。
證據增加可信度，不增加猜測。
整理改善導航，不破壞歷史。
```

所有後續研究都應讓下一個人或下一個 LLM 能在最少重複工作的情況下繼續前進。